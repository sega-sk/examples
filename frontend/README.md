# Frontend (Vite)

From the `examples/frontend` folder:

Install and run dev server:

```bash
npm install
npm run dev
```

Build for production (output goes to `examples/dist`):

```bash
npm run build
```

Vercel notes: after building, static files are in `examples/dist` and will be served by Vercel as the site root.
